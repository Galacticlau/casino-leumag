{
  "name": "casino-escolar",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicación escolar de juegos con moneda ficticia, QR y PostgreSQL embebido, sin Docker.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed:demo": "node scripts/seed-demo.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/session-store.js && node --check src/services/ledger.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.14",
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.2.2",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "express-rate-limit": "^8.1.0",
    "express-session": "^1.18.2",
    "helmet": "^8.1.0",
    "qrcode": "^1.5.4"
  }
}
